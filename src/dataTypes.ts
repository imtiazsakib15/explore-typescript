let a: string = "sa";

let b: number = 6;

let c: boolean = true;

let d: number[] = [2, 4];

let e: [string, number] = ["asdf", 9];

let f: [string, number] = [a, b];

let user: {
  firstName: string;
  middleName: string;
  lastName: string;
  age: number;
  institute?: string;
  readonly subject: string;
} = {
  firstName: "Imtiaz",
  middleName: "Ahmed",
  lastName: "Sakib",
  age: 24,
  subject: "CSE",
};
